{{- if (ne .Values.global.clusterEnv "local") }}
{{- if (and .Values.hybridCloud .Values.hybridCloud.enabled) }}
{{- if (and .Values.hybridCloud.metrics .Values.hybridCloud.metrics.enabled) }}
{{- $monitorData := dict "Release" .Release "Chart" .Chart "Values" .Values "component" "consul-proxy" .name "config" $.Values.hybridCloud.metrics }}
---
{{ include "mintel_common.podMonitor" $monitorData }}
{{- end }}
{{- end }}
{{- end }}
