{{- if (ne .Values.global.clusterEnv "local") }}
{{- if (and .Values.mariadb .Values.mariadb.enabled) }}
{{- if (and .Values.mariadb.metrics .Values.mariadb.metrics.enabled) }}

{{- $monitorData := dict "Release" .Release "Chart" .Chart "Values" .Values "component" "mysqldexporter" "config" .Values.mariadb.metrics }}
{{ include "mintel_common.podMonitor" $monitorData }}
{{- end }}
{{- end }}
{{- end }}
